name: Test SQL Network Connectivity

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test SQL Server port
        run: |
          echo "Testing connectivity to SQL Server..."
          nc -vz -w 10 10.10.4.174 1433
